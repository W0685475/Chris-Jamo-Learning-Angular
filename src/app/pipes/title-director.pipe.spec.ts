import { TitleDirectorPipe } from './title-director.pipe';

describe('TitleDirectorPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleDirectorPipe();
    expect(pipe).toBeTruthy();
  });
});
